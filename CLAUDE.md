# CLAUDE.md

Claude Code Hooks 发布仓库。

## 结构

- `hooks/` — 各 Hook 脚本目录
- 每个 Hook 独立文件夹，包含 `hook.sh` 和 `settings.json`

## 添加新 Hook

1. 在 `hooks/` 下创建新目录
2. 编写 `hook.sh` 和 `settings.json`
3. 更新 `README.md`
4. 提交 PR
